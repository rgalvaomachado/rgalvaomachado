name: Atualizar Calendário Profissional

on:
  schedule:
    # Roda a Action automaticamente uma vez por dia (meia-noite UTC)
    - cron: '0 0 * * *' 
  workflow_dispatch: # Permite rodar manualmente pela aba Actions
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Gerar Gráfico de Atividade Profissional
        # AQUI ESTÁ A NOVA ACTION MAIS ESTÁVEL
        uses: JamesIves/github-activity-readme@v1 
        with:
          # Seu username profissional
          USERNAME: romulosuperlogica
          # O Segredo que armazena a permissão de leitura
          TOKEN: ${{ secrets.GH_TOKEN_PROFISSIONAL }} 
          # O arquivo que a Action irá gerar
          OUTPUT_PATH: "assets/github-activity-graph.svg"
          
      - name: Commit Files (Salvar a nova imagem)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "🤖 Atualizar Calendário Profissional (Action)" || echo "Nenhuma alteração para commitar"
          git push
